{
  "name": "@tokenring-ai/javascript",
  "description": "JS-specific development tools including ESLint auto-fix and package management.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/app": "0.2.0",
    "zod": "^4.3.6",
    "@tokenring-ai/filesystem": "0.2.0",
    "eslint": "^10.1.0"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "type": "module",
  "devDependencies": {
    "vitest": "^4.1.1",
    "typescript": "^6.0.2"
  }
}
